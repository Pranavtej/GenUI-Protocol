# GenUI Protocol Docs

This app provides a Nextra-based documentation site for the GenUI Protocol project.

## Scripts

- `pnpm dev` – start the docs site locally
- `pnpm build` – build the docs site for production
- `pnpm lint` – lint the docs app
